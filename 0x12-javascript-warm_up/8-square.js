#!/usr/bin/node

const myArg = process.argv.slice(2);
let num = Number(myArg[0]);
for (let i = 0; i < num; i++)
{
let result = [];
for (let j = 0; j < num; j++){
let sign = "X";
result.push(sign)
}
let m = result.join("");
console.log(m);
}
